import { History } from "../types";

interface HistoryProps {
  historyList: History[];
}

export default function History({ historyList }: HistoryProps) {
    return (
        <div>
            <ul>
                <li></li>
            </ul>
        </div>
    )

}